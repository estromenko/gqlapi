type User {
    id: ID!
    email: String!
    username: String!
    password: String!
}


type Query {
    user(id: ID!): User
    users: [Users!]
}

type Mutation {
    createUser(email: String!, username: String!, password: String!): User
}

schema {
    query: Query
    mutation: Mutation
}

